export const isInteger = (number: number): boolean => {
  return Number.isInteger(number);
};

// export const isInRange = (number: number, minInclusive: number, maxExclusive: number): boolean => {
//   return 
// };